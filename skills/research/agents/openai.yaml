interface:
  display_name: "Adaptive Research"
  short_description: "Skill-only research with optional tracked depth"
  default_prompt: "Use $research to answer this question with one cited root loop by default, admitting tracked state, MCP, scouts, or verification only when the evidence or requested outcome needs them."
