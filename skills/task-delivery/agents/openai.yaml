interface:
  display_name: "Task Delivery"
  short_description: "Quick, tracked, or verified software delivery"
  default_prompt: "Use $task-delivery to implement and test this task through the cheapest safe tier, admitting durable tracking, slices, handoff, or independent verification only when needed."
