interface:
  display_name: "Project Start"
  short_description: "Fast project foundation and living docs"
  default_prompt: "Use $project-start to prepare this repository and keep its canonical documentation current with minimal ceremony."
policy:
  allow_implicit_invocation: true
